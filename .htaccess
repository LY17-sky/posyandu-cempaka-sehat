# Prevent directory listing and protect application files
Options -Indexes
RewriteEngine On
RewriteBase /

# Redirect simple module requests to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [L,QSA]
